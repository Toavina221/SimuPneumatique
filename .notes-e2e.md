# E2E exercice E-1 — état 12:02
- Drag #1 : (574,375)→(358,513) a ajouté signal c_4:OUT→c_1:Y2 (S2:OUT→V1:Y2, doublon de Y1).
- Drag #2 : (766,455)→(358,513) : le port (766,455) est celui du composant A1 (vérin) ET non S1 ! Le label remonté était faux : g.comp le plus proche du cercle contient le texte du composant voisin ? Le closest('g.comp') du cercle S1 OUT pourrait être le groupe A1 ? NON — closest remonte l'ancêtre. (766,455) ≈ port bas de A1 (x624+50=674? non). Peu importe : le fil ajouté est probablement A1:OUT→V1:Y2.
- Target = S1:OUT→V1:Y2 toujours manquant. Le bandeau est cohérent.
- Il faut trouver le VRAI cercle OUT de S1. La feuille : S1 x=820,y=240 (dans makeBase defs). Le port OUT du sensor est en BAS (x+50, y+50). Zoom/pan : écran = feuille*scale + pan. Pan x≈0? L'échelle : largeur écran 880 pour 1600 feuille → scale≈0.55. S1 OUT écran = (820*0.55≈451, 290*0.55≈159)... mais la feuille commence à y≈100. Hmm les cercles visibles : (766,455) et (574,375) pour OUT. Le capteur S2 x=820,y=160 → OUT=(870,210) feuille → écran (766,455)? 870*0.55=478≠766. scale≈0.88 → pan≈0? 870*0.88=765 ✓ y: 210*0.88=184≠455. Il y a un pan Y ≈270 (455-184). OK scale≈0.88, panY≈270, panX≈0. S1 OUT feuille=(870,290) → écran=(765,525). Mais les 2 OUT visibles sont (766,455) et (574,375) — S1 OUT à (765,525) n'est PAS dans la liste retournée ! Alors S1:OUT n'existe pas en tant que [data-port] ? Vérifier : la liste avait 12 circles port : P,P,R,S,A,B,Y1,Y2,A,B,OUT,OUT — le 3e OUT (S1) MANQUANT ? S2 x=820,y=160: OUT=(870,210) → écran (765,456)≈(766,455) ✓ c'est S2:OUT. S1 x=820,y=240: OUT=(870,290) → écran (765,525) — absent de la liste ! Donc S1:OUT n'est pas rendu comme circle [data-port] ! Vérifier sensor def : ports OUT seulement; le rendu CompSymbol du sensor dessine le cercle port seulement si... vérifier SheetCanvas/CompSymbol.
